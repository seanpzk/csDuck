import { render, fireEvent, screen } from "@testing-library/react";
import TaskList from "../src/components/TaskList";

test("TaskList has right UI", () => {
  // render the component on virtual dom
  render(<TaskList />);

  //elements to interact with
});
